package com.example.produits;

import java.util.Date;
import java.util.List;

import org.junit.jupiter.api.Test;
import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.boot.test.context.SpringBootTest;

import com.example.produits.entitys.Categorie;
import com.example.produits.entitys.Produit;
import com.example.produits.repository.ProduitRepository;

@SpringBootTest
class CategoryProduitApplicationTests {

	/*
	 * @Autowired private ProduitRepository produitRepository;
	 * 
	 * @Test public void testCreateProduit() { Produit prod = new
	 * Produit("Iphone",5000.500,new Date(),""); produitRepository.save(prod); }
	 * 
	 * @Test public void testFindProduit() { Produit p =
	 * produitRepository.findById(1L).get(); System.out.println(p); }
	 * 
	 * @Test public void testUpdateProduit() { Produit p =
	 * produitRepository.findById(1L).get(); p.setPrixProduit(3.5);
	 * produitRepository.save(p); System.out.println("produit changed"); }
	 */
	
	
	/*
	 * @Test public void testDeleteProduit() { Produit p =
	 * produitRepository.findById(1L).get();
	 * 
	 * produitRepository.delete(p); System.out.println("produit deleted"); }
	 * 
	 * @Test public void testFindAllProduit() { List<Produit> p =
	 * produitRepository.findAll();
	 * 
	 * 
	 * System.out.println(p); }
	 * 
	 * @Test public void testFindProduitByName() { List<Produit> p =
	 * produitRepository.findByNomProduit("iphone"); System.out.println(p); }
	 * 
	 * @Test public void testFindProduitByNameContains() { List<Produit> p =
	 * produitRepository.findByNomProduitContains("p"); System.out.println(p); }
	 * 
	 * 
	 * @Test public void testfindByNomPrix() { List<Produit> p =
	 * produitRepository.findByNomPrix("iphone",2000.5); System.out.println(p); }
	 * 
	 * @Test public void testfindByCategorie() { //sna3na categorie jdida (exp) w
	 * lawejna biha Categorie cat = new Categorie(); cat.setIdCategory(1L);
	 * List<Produit> p = produitRepository.findByCategorie(cat);
	 * System.out.println(p); }
	 * 
	 * @Test public void testfindByCategorieIdCategory() { //sna3na categorie jdida
	 * (exp) w lawejna biha ; List<Produit> p =
	 * produitRepository.findByCategorieIdCategory(3L); System.out.println(p); }
	 * 
	 * 
	 * @Test public void testfindByOrderByNomProduitAsc() { List<Produit> p =
	 * produitRepository.findByOrderByNomProduitAsc();
	 * 
	 * System.out.println(p);
	 * 
	 * }
	 * 
	 * @Test public void testTrierProduitsNomsPrix() { List<Produit> p =
	 * produitRepository.trierProduitsNomsPrix();
	 * 
	 * System.out.println(p);
	 * 
	 * }
	 */
	
	}
